# cmdlog
# Command Login ( DDNet ) ( Client Login/Password or Username

import startz


LOG= k 
for.LOG(k.log)
 k.log(int(=ported) * log.maim)
   create.label:new.category
Post bool possGet(-.009.99822.1111.00,HashLog)
 Main.Port(188:83002)
  connect.port[188]
   print("Connect is successfully")
  elseif
   print("Connect is Faild")
end

High.Port(83002)
 connect[83002]
  this connect[83002], New.connect (188002)
    allow.Port[83002]
     connect.port[83002]
      disconnect.port[83002]
 create.log for [83002]
   send.file[conf.log]
   prob.log[188]
end
  end

import ddnet

connect.port[83002]
connect.port[188]
set.menu "k.log"
 asm.create.port[833]
